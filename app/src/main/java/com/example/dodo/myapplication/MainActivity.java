package com.example.dodo.myapplication;import android.content.Intent;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.util.Log;import android.view.View;import android.widget.EditText;import android.widget.TextView;import android.widget.Toast;import dao.UsuarioDao;public class MainActivity extends AppCompatActivity {    private EditText edtUsuario, edtSenha;    private UsuarioDao usuarioDao;    static final int NOVO_CADASTRO = 1;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        edtSenha = (EditText) findViewById(R.id.editTextSenha);        edtUsuario = (EditText) findViewById(R.id.editTextLogin);/*        Bundle bundle = getIntent().getExtras();        if(bundle.getString("cadastro")!= null)        {            Toast.makeText(MainActivity.this, bundle.getString("cadastro"), Toast.LENGTH_SHORT).show();        }*/    }    public void goToRegistroNovoUsuario(View v){        Intent it = new Intent(this, CadastroLogin.class);        startActivity(it);        setContentView(R.layout.activity_cadastro_login);    }    public void logarUsuario(View v){        String usuario = edtUsuario.getText().toString();        String senha = edtSenha.getText().toString();        boolean validacao = true;        if(usuario == null || usuario.equals("")) {            validacao = false;            edtUsuario.setError(getString(R.string.login_usuario));        }        if(senha == null || senha.equals("")) {            validacao = false;            edtSenha.setError(getString(R.string.login_senha));        }        if(validacao){            usuarioDao = new UsuarioDao(this);            if(usuarioDao.logar(usuario,senha)){                this.goToStickNotes(v);            }            else{                Toast.makeText(MainActivity.this,"Usuário ou senha inválidos", Toast.LENGTH_LONG).show();            }        }    }    private void goToStickNotes(View v){        Intent it = new Intent(this, StickNotes.class);        //startActivity(it);        startActivityForResult(it, NOVO_CADASTRO);        setContentView(R.layout.activity_stick_notes2);    }    @Override    protected void onActivityResult(int requestCode, int resultCode, Intent data) {        if(requestCode == NOVO_CADASTRO){            if(requestCode == RESULT_OK){                Toast.makeText(MainActivity.this,"Usuário cadastrado com sucesso!", Toast.LENGTH_LONG).show();            }        }        super.onActivityResult(requestCode, resultCode, data);    }}