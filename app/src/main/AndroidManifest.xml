<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.dodo.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".CadastroLogin">
        </activity>
        <activity android:name=".StickNotesCrud">
        </activity>
        <activity
            android:name=".StickNotes"
            android:label="@string/title_activity_stick_notes"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".AvisoCadastroOk">
        </activity>
        <activity android:name=".AcaoStickNote">
        </activity>
    </application>

</manifest>